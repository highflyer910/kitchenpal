import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#B35B38',
    },
    secondary: {
      main: '#FCF8E8',
    },
    background: {
      default: '#F2D09F',
      paper: '#C7954A',
    },
    text: {
      primary: '#252813',
    },
  },
});

export default theme;
