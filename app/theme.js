import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#B35B38',
    },
    secondary: {
      main: '#FCF8E8',
    },
    background: {
      default: '#F2D09F',
      paper: '#C7954A',
    },
    text: {
      primary: '#5E6738',
    },
  },
});

export default theme;
