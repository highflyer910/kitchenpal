import { createTheme } from '@mui/material/styles';

const theme4 = createTheme({
  palette: {
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#FFFFFF',
    },
    background: {
      default: '#F5F5F5',
      paper: '#E0E0E0',
    },
    text: {
      primary: '#363B20',
      secondary: '#1a1c0f',
    },
  },
});

export default theme4;