import { createTheme } from '@mui/material/styles';

const theme1 = createTheme({
  palette: {
    primary: {
      main: '#610023',
    },
    secondary: {
      main: '#FCF8E8',
    },
    background: {
      default: '#FFD8B1',
      paper: '#C27F79',
    },
    text: {
      primary: '#3d0116',
      secondary: '#1a0009',
    },
  },
});

export default theme1;
